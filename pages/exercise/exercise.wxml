<!--pages/exercise/exercise.wxml-->

<view class="exercise-wrapper">
    <CategoryItem
        wx:for="{{ examItemInfo }}"
        wx:key="unique"
        examItemInfo="{{ item }}"
    >
    </CategoryItem>
</view>
